{
  "compilerOptions": {
    "target": "ESNext", // Use the latest ES version for modern browser compatibility.
    "useDefineForClassFields": true, // To enable `define` for class fields to match the standard.
    "lib": ["ES2020", "DOM", "DOM.Iterable"], // Include modern JavaScript and DOM types.
    "module": "ESNext", // Use ESNext modules for native support in Bun.
    "skipLibCheck": true, // Skip library checks to speed up compilation.

    /* Bundler Mode */
    "moduleResolution": "bundler", // Use Bun's efficient module resolution.
    "allowImportingTsExtensions": true, // Allows `.ts` extensions in imports.
    "isolatedModules": true, // Optimizes Bun for working with isolated modules.
    "moduleDetection": "force", // Force module detection for all files.
    "noEmit": true, // Bun will handle emitting files, so TypeScript doesn’t need to.

    /* JSX/React Settings */
    "jsx": "react-jsx", // For React JSX support.

    /* Linting & Code Quality */
    "strict": true, // Enable strict type-checking.
    "noUnusedLocals": true, // Warning on unused local variables.
    "noUnusedParameters": true, // Warning on unused parameters.
    "noFallthroughCasesInSwitch": true, // Avoid fall-through in switch cases.

    /* Bun-Specific Settings */
    "types": ["vite/client", "bun"] // Include Bun and Vite-specific types.
  },
  "include": ["src"] // Include all source files in the `src` directory.
}
